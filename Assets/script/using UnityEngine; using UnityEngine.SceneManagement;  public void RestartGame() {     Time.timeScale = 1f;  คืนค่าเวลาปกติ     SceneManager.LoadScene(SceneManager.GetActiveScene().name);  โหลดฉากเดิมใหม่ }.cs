using UnityEngine;

public class usingUnityEngine;usingUnityEngine.SceneManagement;publicvoidRestartGame(){Time.timeScale=1f;คืนค่าเวลาปกติSceneManager.LoadScene(SceneManager.GetActiveScene().name);โหลดฉากเดิมใหม่} : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
